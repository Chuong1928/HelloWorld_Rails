
<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="outer w-50 m-auto p-5 ">
    <div class="outer">
        <div class="form-group row mb-4">
            <label for="taskname" class="col-form-label col-lg-2">Name</label>
            <div class="col-lg-10">
                <%= form.text_field :name, placeholder: 'Nhập UserName...' ,class: 'form-control title_category'  %>
            </div>
        </div>
      
        <div class="form-group row mb-4">
            <label for="taskbudget" class="col-form-label col-lg-2">Email</label>
            <div class="col-lg-10">
                <%= form.text_field :email ,placeholder: 'Nhập Email...' ,class: 'form-control peralink_category'  %>
            </div>
        </div>
        <div class="row justify-content-center">
            <div class="col-lg-10 d-flex justify-content-center">
                <button type="submit" class="btn btn-primary new-post mx-2">Submit</button>
                <%= link_to "Hủy", users_path , class: "btn btn-secondary mx-2" %>
                
            </div>
        </div>
    </div>
</div>
<% end %>

